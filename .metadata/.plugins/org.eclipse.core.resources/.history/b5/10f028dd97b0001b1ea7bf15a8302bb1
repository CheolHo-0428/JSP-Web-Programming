package ch04.com.dao;

import java.io.Serializable;

public class GuGuDan implements Serializable {
	
	public String process(int num) { 
		String gugudan = "";
		for(int i=1; i<10; i++) { 
			String gugu = num + " * " + i + " = " + (num*i) + "<br>";
			gugudan += gugu;
		}
		return gugudan;
	}
}
